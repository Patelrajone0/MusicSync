# Workspace Rules for Antigravity

## Automatic Commits
- After completing every requested update or feature implementation and verifying that the build passes, automatically stage, commit (using descriptive conventional commit messages), and push the changes to GitHub (`git push origin main`).
